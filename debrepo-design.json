{
    "_id": "_design/example",
    "views": {
        "foo": {
            "map": "function(doc) {if (doc._id.indexOf(\"collchange:\") == 0) {emit([doc.suite, doc.file], [doc.revision, doc.present]); } }",
            "reduce": "function(key, values, rereduce) {rev = values[0][0]; present = values[0][1]; for (i = 1; i < values.length; i++) { if (values[i][0] > rev) { present = values[i][1]; } } return [rev, present]; }"
        }
    }
}


